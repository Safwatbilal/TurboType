import Login from "@/components/Login";
export const metadata = {
  title: "Login | TurboType",
  description: "Login to TurboType and start improving your typing skills.",
};

const LoginPage = () => {
  return <Login />;
};

export default LoginPage;
